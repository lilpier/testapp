<form method="post" action="{{ path('app_artisty_delete', {'id': artisty.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ artisty.id) }}">
    <button class="btn">Delete</button>
</form>
