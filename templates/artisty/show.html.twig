{% extends 'base.html.twig' %}

{% block title %}Artisty{% endblock %}

{% block body %}
    <h1>Artisty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ artisty.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ artisty.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_artisty_index') }}">back to list</a>

    <a href="{{ path('app_artisty_edit', {'id': artisty.id}) }}">edit</a>

    {{ include('artisty/_delete_form.html.twig') }}
{% endblock %}
