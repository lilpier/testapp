{% extends 'base.html.twig' %}

{% block title %}Artisty index{% endblock %}

{% block body %}
    <h1>Artisty index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for artisty in artisties %}
            <tr>
                <td>{{ artisty.id }}</td>
                <td>{{ artisty.name }}</td>
                <td>
                    <a href="{{ path('app_artisty_show', {'id': artisty.id}) }}">show</a>
                    <a href="{{ path('app_artisty_edit', {'id': artisty.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_artisty_new') }}">Create new</a>
{% endblock %}
